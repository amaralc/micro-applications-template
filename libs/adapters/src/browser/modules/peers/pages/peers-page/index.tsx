import { PeersList } from '../../components/peers-list';

export const PeersPage = () => {
  // const searchParams = useSearchParams();
  // const subjectsSlugs = useMemo(() => {
  //   const localSubjectsSlugs = searchParams.getAll('subjects[]');
  //   return localSubjectsSlugs;
  // }, [searchParams]);

  return <PeersList />;
};
